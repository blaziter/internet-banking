#include "card.hpp"

#include <iostream>
#include <string>

/* Card::Card(int card_number, tm expiry_date, unsigned char status, unsigned char security_number)
    : card_number(card_number), expiry_date(expiry_date), status(status), security_number(security_number) {}
 */
void Card::cardDetails() {}